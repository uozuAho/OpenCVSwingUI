group 'uozu.aho'
version '0.1'

apply plugin: 'java'

sourceCompatibility = 1.7

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: 'libs', include: 'opencv-300.jar')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
